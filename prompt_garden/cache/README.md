# Prompt Garden Cache

This directory is reserved for rebuildable Prompt Garden caches.

Expected use:

- embeddings
- similarity helpers
- other derived review accelerators

Cache files are local/generated artifacts and should not be tracked by default.
